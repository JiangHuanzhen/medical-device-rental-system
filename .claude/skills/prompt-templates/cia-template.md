---
name: 变更影响分析提示词模板
description: 9维度CIA变更影响分析的提示词模板
type: prompt-template
reusable: true
parameters:
  - CR_DOC: 变更需求文档
  - CURRENT_BASELINE: 当前基线版本
  - MDS_PATH: 模块划分方案
  - DTS_PATH: 依赖拓扑
  - TLCD_PATH: 三层约束设计
---

# 变更影响分析 (CIA) 提示词模板

## 适用场景
任何接受变更请求后、代码修改前的系统化影响评估。适用范围：所有有正式设计产物的项目。

## 使用方法
将 CR 文档和当前设计产物填充后调用 LLM。

## 模板

```
你是CIA专家。基于CR文档和当前设计产物，执行9维度变更影响分析。

## CR（变更请求）
{CR_DOC}

## 当前基线
基线版本: {BASELINE_VERSION}

## 当前设计产物
### MDS
{MDS}

### DTS  
{DTS}

### TLCD
{TLCD}

## 9维度评估（逐项标注受影响程度：无/轻微/中等/严重）
1. **需求层面**: SRS中哪些需求条目受影响？是否需要更新RTM？
2. **架构层面**: 是否影响ASD架构约束或分层规则？是否需要新增/修改ADR？
3. **模块层面**: 涉及哪些模块的修改？模块间依赖是否变化？
4. **契约层面**: OpenAPI接口的请求体/响应体/错误码是否变化？是否需要递增x-version？
5. **代码层面**: 需要修改哪些源文件？新增哪些文件？
6. **数据层面**: 数据库Schema/索引/缓存策略是否需要变更？
7. **兼容性层面**: 旧版本客户端是否仍能正常工作？是否需要API版本升级？
8. **测试层面**: 哪些已有测试用例会受影响？需新增哪些测试？
9. **部署运维层面**: CI/CD配置/监控告警/部署顺序是否需要调整？

## 输出格式
生成完整CIA报告，包含：
- 9维度评估表（每维度：受影响程度 + 具体说明）
- 影响路径（沿DTS依赖拓扑追溯）
- 变更实施建议（推荐的实施顺序和风险缓解措施）
```

## 不适用场景
- 全新项目初始开发（无需变更分析）
- 无正式设计产物的项目（缺少影响分析的基准）
